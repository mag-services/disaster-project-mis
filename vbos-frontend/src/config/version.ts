/**
 * Release string for support and handover (NDMO).
 * Keep in sync with Django:
 * - `DRMIS_VERSION_DISPLAY` (or `DRMIS_API_VERSION` + `DRMIS_BUILD` env) in `vbos/config/common.py`.
 */
export const DRMIS_VERSION_DISPLAY = "v1.0.0 · Build 2026.03.21";
