# VBoS Backend – Environment Template
# Copy to .env and fill in values. Never commit .env.

# ─── Required ─────────────────────────────────────────────────────────────
# SECRET_KEY for Django (sessions, CSRF, etc.). Use a long random string in production.
DJANGO_SECRET_KEY="local-dev-secret-change-in-production"

# ─── Configuration ───────────────────────────────────────────────────────
# Django configuration class: Local | Production
# DJANGO_CONFIGURATION="Local"

# Enable debug mode (stack traces, detailed errors). Disable in production.
# DJANGO_DEBUG=false

# ─── Database (PostgreSQL + PostGIS) ───────────────────────────────────────
# Default when using docker-compose: postgres service
# DJANGO_DB_URL="postgis://postgres:postgres@postgres:5432/vbos"

# Override Postgres connection (used by wait_for_postgres.py)
# POSTGRES_HOST=postgres
# POSTGRES_DB=vbos
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_CONN_MAX_AGE=600
# POSTGRES_CHECK_TIMEOUT=30
# POSTGRES_CHECK_INTERVAL=1

# ─── File Storage (S3 / DigitalOcean Spaces) ──────────────────────────────
# Leave empty for local dev (files stored in ./media/)
# DJANGO_AWS_ACCESS_KEY_ID=
# DJANGO_AWS_SECRET_ACCESS_KEY=
# DJANGO_AWS_STORAGE_BUCKET_NAME=

# Optional: Custom S3 endpoint (e.g. DigitalOcean Spaces)
# DJANGO_AWS_S3_ENDPOINT_URL="https://syd1.digitaloceanspaces.com"

# ─── Cache ────────────────────────────────────────────────────────────────
# Default: LocMemCache. For production, use DatabaseCache or RedisCache.
# DJANGO_CACHE_BACKEND="django.core.cache.backends.locmem.LocMemCache"
# DJANGO_CACHE_LOCATION=vbos-default
# DJANGO_CACHE_TIMEOUT=300

# For DatabaseCache: uncomment and run "python manage.py createcachetable"
# DJANGO_CACHE_BACKEND="django.core.cache.backends.db.DatabaseCache"
# DJANGO_CACHE_LOCATION=vbos_cache

# For Redis: DJANGO_CACHE_BACKEND="django.core.cache.backends.redis.RedisCache"
# DJANGO_CACHE_LOCATION="redis://127.0.0.1:6379/1"

# ─── API ───────────────────────────────────────────────────────────────────
# Default API page size (default: 20)
# DJANGO_PAGINATION_LIMIT=20

# Restrict dataset catalog by User.organisation + owning_organisation + shares (GGGI / MoCCA).
# Default unset/false = all authenticated users see the full published catalog (legacy behaviour).
# VBOS_ORGANISATION_SCOPING=true

# ─── Auth / 2FA (local dev) ───────────────────────────────────────────────
# Skip email OTP on login (password-only). Useful when MailHog/SMTP is not wired.
# DISABLE_2FA_GLOBALLY=true
#
# With Docker Compose MailHog: in Admin → SMTP settings use host `mailhog`, port `1025`,
# TLS off (not `localhost` — that is the web container itself).

# ─── AI (optional) ───────────────────────────────────────────────────────
# Natural-language “Ask the map” planner (OpenAI). If unset, POST /api/v1/ai/map-query/ returns 503.
# OPENAI_API_KEY=
# AI_OPENAI_API_KEY=
# AI_MAP_QUERY_MODEL=gpt-4o-mini
